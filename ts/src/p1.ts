export function isPalindrome(s: string): boolean {
  return false;
}

export function reverseWordsInString(s: string): string {
  return s;
}

// note: You can solve this with `return eval(s)` but you shouldn't, that's not the point!
// Also, using eval in production code is generally bad practice.
export function doCalculation(s: string): number {
  return 0;
}
